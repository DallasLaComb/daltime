meta {
  name: Assign Employee Location
  type: http
  seq: 2
}

post {
  url: {{base_url}}/org-admin/employees/{{employee_id}}/locations
  body: json
  auth: none
}

body:json {
  {
    "location_id": "<location_id>"
  }
}
